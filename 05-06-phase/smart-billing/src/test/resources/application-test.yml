spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  ai:
    vertex:
      ai:
        gemini:
          project-id: smartbilling-ia
          location: us-central1

          chat:
            options:
              model: gemini-1.5-flash

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true